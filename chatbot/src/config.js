import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [createChatBotMessage('hi! search by description - "search: <search for> get info on attack - infoOn: <attack name> check with total viruse - Check the: <singnature to check>"')]
}

export default config